const modalInfoCondition: Object = require('./modal-info-conditions.json');
const productPaymentSection: Object = require('./product-payment-section.json');

export const es = {
  modalInfoCondition,
  productPaymentSection,
};
